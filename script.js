const start=document.getElementById('start'),startBtn=document.getElementById('startBtn'),buky=document.getElementById('buky'),bubble=document.getElementById('bubble'),toast=document.getElementById('toast');let ctx=null,started=false,soundOn=true;function showBubble(t='Ahoj kamaráde!'){bubble.textContent=t;bubble.classList.remove('show');void bubble.offsetWidth;bubble.classList.add('show');speak(t)}function showToast(t){toast.textContent=t;toast.classList.remove('show');void toast.offsetWidth;toast.classList.add('show')}function beep(f,t,d,type='sine',gain=.035){if(!ctx||!soundOn)return;const o=ctx.createOscillator(),g=ctx.createGain();o.type=type;o.frequency.setValueAtTime(f,t);g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(gain,t+.03);g.gain.exponentialRampToValueAtTime(.0001,t+d);o.connect(g).connect(ctx.destination);o.start(t);o.stop(t+d+.05)}function speak(t){if(!soundOn||!('speechSynthesis'in window))return;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(t);u.lang='cs-CZ';u.rate=.95;u.pitch=1.25;window.speechSynthesis.speak(u)}function introBuky(){buky.className='';void buky.offsetWidth;buky.classList.add('intro');setTimeout(()=>{buky.classList.add('wave');showBubble('Ahoj kamaráde!')},1550);setTimeout(()=>{buky.classList.remove('intro','wave');buky.classList.add('idle')},2300)}function jumpBuky(){buky.classList.remove('idle','jump');void buky.offsetWidth;buky.classList.add('jump');setTimeout(()=>{buky.classList.remove('jump');buky.classList.add('idle')},560)}function startAudio(){if(started)return;started=true;start.style.display='none';ctx=new(window.AudioContext||window.webkitAudioContext)();const now=ctx.currentTime+.05;[392,523,659,784,659,523,392].forEach((f,i)=>beep(f,now+i*.16,.14,'triangle',.055));setInterval(()=>{const t=ctx.currentTime;beep(1450+Math.random()*500,t,.07,'sine',.022);beep(1850+Math.random()*500,t+.11,.06,'sine',.017)},1500);const m=[262,330,392,330,294,349,392,523];let s=0;setInterval(()=>{beep(m[s%m.length],ctx.currentTime,.18,'triangle',.012);s++},520);introBuky()}startBtn.addEventListener('click',startAudio);document.body.addEventListener('touchstart',startAudio,{once:true});buky.addEventListener('click',()=>{jumpBuky();showBubble('Ahoj kamaráde!')});document.querySelectorAll('.hotspot').forEach(btn=>{btn.addEventListener('click',()=>{const a=btn.dataset.action;if(ctx)beep(520,ctx.currentTime,.08,'square',.025);if(a==='play')showToast('Hru spustíme v další verzi 🐂');if(a==='character')showToast('Výběr postavy připravíme');if(a==='shop')showToast('Obchod bude brzy dostupný');if(a==='settings'||a==='sound'){soundOn=!soundOn;showToast(soundOn?'Zvuk zapnutý 🔊':'Zvuk vypnutý 🔇')}})});if('serviceWorker'in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(()=>{}))}